const INITIAL_STATE = {
    // Put your global app state values here
}

export default (state = INITIAL_STATE, action) => {
    switch (action.type) {
        // Put your global app reducers here
        default:
            return state
    }
}
